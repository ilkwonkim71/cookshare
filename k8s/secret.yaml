apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: cookshare
  labels:
    app.kubernetes.io/part-of: cookshare
    app.kubernetes.io/component: backend
type: Opaque
stringData:
  # 로컬 테스트 전용 값. 운영에서는 절대 사용 금지(리스크 R1).
  JWT_SECRET: 'dev-secret-change-me'
