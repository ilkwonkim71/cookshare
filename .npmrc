# pnpm workspace settings.
# Local packages are linked via the `workspace:*` protocol declared in each
# package.json, so no extra linking config is needed here.
# Native build scripts that are allowed to run are whitelisted under
# "pnpm.onlyBuiltDependencies" in the root package.json (better-sqlite3, esbuild).
