{
  "name": "@cookshare/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared API contract (types + constants) between backend and frontend",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "clean": "tsc -b --clean",
    "test": "jest",
    "test:coverage": "jest --coverage"
  }
}
